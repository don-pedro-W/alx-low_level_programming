#include <stdio.h>
/**
 * main- the entry point for code
 *
 * Return: o if error, no-zero if error
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
